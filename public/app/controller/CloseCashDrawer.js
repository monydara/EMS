Ext.define('ExtDesktop.controller.CloseCashDrawer', {
    extend: 'Ext.app.Controller',
    stores: [
    ],
    views: [
             'module.closeCashDrawer.Index',
         
    ],  
    init: function(app) {
        
        this.control({

           
        });
        
    },
});

